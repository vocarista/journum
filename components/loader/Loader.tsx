// components/Loading.js
import styles from './loader.module.css'
const Loader = () => {
    return (
        <div className={`${styles.container}`}><div className = {`${styles.loaderLine}`}></div></div>
    )
};
  
  export default Loader;
  